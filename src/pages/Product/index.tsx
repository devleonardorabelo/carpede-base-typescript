import React from 'react';
import { ScrollView } from 'react-native-gesture-handler';

import styles from './styles';

const View: React.FC = () => {
  return <ScrollView style={styles.section}></ScrollView>;
};

export default View;
